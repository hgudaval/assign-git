What is a gitignore file and what should it include for a Java project?Git ignore file in the git repository indicates git to not to track certain files and folders.For Java it should consist of the files which are created by Eclipse and VScode which we use during project development.What does fast-forward in Git mean?A fast forward is what Git does when you merge or rebase against a branch that is ahead the one you have checked-out.Why should you have different branches?Having different branches helps to stage the changes to the master when required instead of directly making changes to master will make the whole project a messIn a project how many remote Git repositories do you usually have?We use just one repository but we can add as many as we want